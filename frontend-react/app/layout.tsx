import type { Metadata } from 'next';
import './globals.css';
import AppShell from '@/components/layout/AppShell';

export const metadata: Metadata = {
  title: 'Crosscheck — Fact Verification & Cross-Document Reasoning',
  description: 'Ingest PDFs, extract verifiable claims, and reason about cross-document corroborations and contradictions.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
